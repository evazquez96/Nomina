function validarContenidoDeCeldas(Object) {
    /**
     * Se pasara como parametro el Object el cual 
     * contendra el valor de las celdas y el 
     * td el cual estara en rojo, si el background-color
     * es red, implica que el valor de la celda esta mal,
     * esta función se encargara de revisar la propiedad
     * background-color de cada celda, si se encuentra 
     * alguna en color rojo, se abrira una ventana 
     * emerjente donde se visualizaran los registros que
     * tienen campos invalidos.
     ***/
}